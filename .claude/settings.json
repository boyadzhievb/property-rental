{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(ts|tsx|js|jsx)$' && cd /Users/boyadboz/REPOS/property-rental && npm test 2>&1 | tail -5; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Running unit tests..."
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "cd /Users/boyadboz/REPOS/property-rental && npx tsc --noEmit 2>&1 | tail -10 && npm test 2>&1 | tail -5 || true",
            "timeout": 60,
            "statusMessage": "Running post-commit checks (types + tests)..."
          }
        ]
      }
    ]
  }
}
